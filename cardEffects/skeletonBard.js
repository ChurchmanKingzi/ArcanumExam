/**
 * Skeleton Bard — Copy an ally Familiar's effect and perform it.
 * Fizzles if no other Familiar with a usable effect exists.
 */
export default function skeletonBard(familiar, context) {
  return { effectType: 'bard-copy' };
}
