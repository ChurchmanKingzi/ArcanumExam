// Halberd — passive equip
// +4 ATK to the equipped student.
// When attacking, all targeting restrictions are ignored — any living non-self
// target on the board is always eligible (implemented via halberAttack flag).
export const halberdEffect = { passive: true, effect: null };
