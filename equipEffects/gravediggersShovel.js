// Gravedigger's Shovel — passive equip
// Prevents all Familiars in any discard pile from leaving (same lock as Mummy Guards).
// Implemented by extending hasMummyGuardsLock() in server.js.
export const gravediggersShovelEffect = { passive: true, effect: null };
