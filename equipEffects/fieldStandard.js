// Field Standard — passive equip
// Whenever the equipped Student takes any damage, if the player has any
// tapped Familiars, they may select up to X of them to untap immediately
// (X = number of Field Standards equipped). Cancellable. Simultaneous
// prompts if multiple players trigger at the same time.
export const fieldStandardEffect = {
  passive: true,
  effect: null,
};
